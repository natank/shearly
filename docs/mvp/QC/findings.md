# QC Findings Log

Running record of QC FAILs that are real product gaps, kept here until each is triaged into a milestone plan or a tracked backlog item. Not a substitute for [README.md](./README.md) run logs — this file is for findings that outlive a single run and need a decision (fix now, defer, or accept as a named hole).

**Do not** log BLOCKED (lab/env issues) or tester mistakes here — only FAILs that are confirmed real, per [README.md](./README.md) §2 verdicts.

---

## How to add a finding

1. Append a row to the table below. Use the next `QCF-###` id.
2. Link the procedure id (e.g. `M2-T03`) and the milestone STD it came from.
3. Keep **Summary** to one line; put root cause / repro detail in **Notes**.
4. Status starts `Open`. Update to `Fixed` (link the PR), `Backlog` (link the issue/plan item), or `Accepted` (named hole, say where it's documented) once triaged.

| ID | Procedure | Severity | Summary | Status | Notes |
|---|---|---|---|---|---|
| QCF-001 | M1-T05 step 9 | Must | Re-submitting a used/expired/unknown reset token showed the generic "Email or password is incorrect" instead of a reset-specific message | Fixed | Root cause: `ResetConfirmForm` discarded the response body and ignored `translationKey`. Backend token single-use enforcement was already correct. Fixed on `fix/m1-reset-invalid-token-message`; added missing `resetInvalid` i18n string (en/he). |
| QCF-002 | M2-T02 step 3 | Must | Submitting a vetting packet for review showed `pending_review` in the UI but sent no Mailpit mail | Fixed | Root cause: `CatalogService.submit()`/`.decide()` never called a mailer — M1's SMTP helper was wired into `IdentityService` only. Fixed on `fix/m2-t02-submit-decision-mail`; added service-level and HTTP-level regression tests. |
| QCF-003 | M2-T03 | Should | Admin vetting queue cards show only a generic status string ("Waiting for interview") — no provider name, email, or id. With 2+ pending providers, rows are indistinguishable | Open | M2-P6 plan spec required "queue **+ detail** + document open... + decision form"; shipped UI has none of the detail/document-open parts. QC doc (`std-m2-supply.md` M2-T04) already separately flags the missing "Open document" button as a known gap — this is the same shortfall extended to provider identity on the queue card itself. |
| QCF-004 | CI gates 1–8, "E2E both locales" | Must | `demand-smoke.spec.ts` ("hebrew visitor finds an in-radius provider and slots") intermittently failed on unrelated PRs with the wrong provider id in the URL assertion | Fixed | Not found via manual QC — surfaced as CI red while merging an unrelated PR. Root cause: the test clicked the page-wide first "open profile" link instead of the link inside its own seeded provider's card; each CI retry (`retries: 2`) re-seeds a new listed provider without cleaning up the prior attempt's row, so once 2+ listed providers coexist in radius, `.first()` grabs the wrong one. Fixed on `fix/e2e-demand-smoke-profile-link-scope` by scoping the click to the list item containing the seeded provider's unique name. Verified locally by reproducing the pollution (4 coexisting listed providers) and re-running clean. |
| QCF-005 | M3-T09 step 2 | Must | Saving a customer address threw an uncaught `TypeError: Cannot read properties of null (reading 'reset')` in `addresses.tsx:39`, though the address still saved and appeared after a manual reload | Fixed | Root cause: `AddressBook`'s `onSave` read `event.currentTarget` a second time *after* an `await fetch(...)` — by then the SyntheticEvent's `currentTarget` is already null, since the event has finished dispatching. Fixed on `fix/m3-t09-address-form-reset-crash` by capturing the form element into a local variable before the `await`. Added `apps/web-e2e/src/addresses-smoke.spec.ts` (register → save address → assert no `pageerror` and the row appears); confirmed it fails on the pre-fix code and passes on the fix. |
| QCF-006 | M3-T09 (`GET /catalog/public/<random/malformed id>`) | Must | `GET /catalog/public/234243423243` returned `500 Internal Server Error` instead of the spec'd `404` for an unlisted/random/malformed provider id | Fixed | Root cause: every `:providerId`/`:serviceId`/`:docId` route param in `catalog-routes.ts` was passed straight into a `uuid`-typed SQL `WHERE` clause; a non-UUID string throws a raw Postgres cast error that the global handler only maps to a generic `500` (it only special-cases `AppError` subclasses). Affected 7 routes: public provider profile, service slots, portfolio doc, and all three admin vetting routes. Fixed on `fix/m3-t09-catalog-uuid-param-500` by adding a `requireUuidParam` helper that validates the param shape up front and throws `NotFoundError` (→ 404) on a bad shape, applied at every affected call site. Added regression tests in `catalog-public.spec.ts` and `catalog.spec.ts` covering malformed ids on all 7 routes. |
| QCF-007 | M3-T13 | Should | M3-T13's WAIVE justification ("covered by unit tests") is only half true: `defaultDistanceThenRating`'s sort logic is unit-tested (`libs/domain/ranking/src/ranking.spec.ts:99`), but the `try/catch` in `apps/api/src/discovery.ts` that actually triggers the fallback on a ranker throw or `rankingTimeoutMs` timeout has zero test coverage — no spec references `composeDiscovery`, the `Promise.race` timeout, or the catch branch | Open | Not a functional bug — code review of `discovery.ts`'s try/catch shows it's correctly wired (catches both a thrown error and the timeout race, falls back to the tested sort). WAIVED for M3 per the doc's own allowance; logged here so the missing integration test isn't lost. Would need injecting a throwing/timing-out `ProviderRanker` into `composeDiscovery` in a real test to close. |
| QCF-008 | M4-T02 step 2 | Must | `POST /webhooks/stripe` 404'd on every event `stripe listen` forwarded — the route was never mounted in `apps/api/src/app.ts` | Fixed | Root cause: `handleStripeWebhook()` (signature verification, idempotent by `event.id` via `payments.webhook_events`) was fully built and unit-tested in `libs/services/payments`, but the M4 plan's own layout section named `apps/api/src/webhooks-routes.ts` and it was never created — a pure wiring gap. Fixed on `fix/m4-p2-stripe-webhook-route`, merged as PR #64. Verified live against a real Stripe test-mode account: events now `200`, land in `payments.webhook_events`. |
| QCF-009 | M4-T02 step 1 | Must | Every booking confirm failed `402 authorizationFailed` once real Stripe test-mode keys were configured (previously invisible in stub mode) | Fixed | Root cause: `libs/ui/feature-booking/src/confirm.tsx` hardcoded `paymentMethodId: 'pm_card_default'` — not a real Stripe object. Design §8.1 and §13 decision #10 require real Stripe Elements card collection (NFR-SEC-001, PAY-001); it was never built despite booking submission otherwise being fully wired to a real server-side authorize call. Server needed no changes — `POST /bookings` already accepted a `paymentMethodId` string. Fixed on `fix/stripe-elements-booking-confirm` (branch exists, not yet a PR at time of writing): `GET /payments/config` exposes the publishable key, `payment-fields.tsx` mounts a real `<CardElement>` and turns entered card data into a real `pm_...` id via `stripe.createPaymentMethod()`. Falls back to a stub id when no publishable key is configured. Verified: full API + `feature-booking` test suites green, `next build` clean, real Stripe test-mode booking creation confirmed end-to-end via direct API calls with real `pm_...` ids. Not yet verified via an actual browser click-through typing into the `CardElement` iframe. |
| QCF-010 | M4-T08 step 1 (also blocks M4-T10 steps 3-4) | Must | No HTTP route exists for provider-initiated cancel (BOK-006) or customer-reported provider no-show (BOK-008, the provider-no-show half) — both P0/Must, both named explicitly in the master demo script and M4's own exit checklist | Fixed | Root cause: `ProviderCancels` and `CustomerReportsProviderNoShow` are fully implemented and exhaustively unit-tested in `libs/domain/booking-state-machine` (confirmed both transitions pass in the state machine's own table test), but neither was ever wired into an HTTP route — `apps/api/src/booking-provider-routes.ts` only mounted accept/decline/complete/no-show(customer-reported)/provider-view/earnings, and `apps/api/src/booking-routes.ts` (customer-facing) had no equivalent for reporting a provider no-show. Fixed on `fix/qcf-010-provider-cancel-and-noshow-routes`: added `PATCH /bookings/:id/provider-cancel` (provider-facing, `ProviderCancels`) and `PATCH /bookings/:id/provider-no-show` (customer-facing, `CustomerReportsProviderNoShow`) — deliberately distinct paths from the existing customer `/bookings/:id/cancel` and provider `/bookings/:id/no-show` routes, since reusing either path for both actors/events would have silently collided (Hono resolves to whichever route mounts first; `booking-routes.ts` mounts before `booking-provider-routes.ts` in `app.ts`) — confirmed this collision was real by writing the naive same-path version first and watching the wrong actor's handler win. New regression spec `apps/api/src/booking-provider-cancel-noshow-flow.spec.ts` (5 tests: full-refund-no-fee + standing event for provider cancel, cross-actor/cross-tenant 403s, full-refund + standing event + before-slot-start guard for provider no-show, cross-tenant 403). Re-ran M4-T08 and M4-T10 steps 3-4 live against real Stripe test-mode after the fix: both PASS (`canceled`/`amount_received: 0`, `booking.standing_events` rows confirmed). Full API suite green (63/63). |
| QCF-011 | M4-T05 step 2 | Should | `payments.authorizations.status` is never updated after a successful cancel, capture, or refund — stays at whatever it was set to at `authorize`/`setup` time (almost always `AUTHORIZED`) forever after | Fixed | Confirmed the real Stripe-side operations are all correct (verified `cancelAuthorization`'s effect directly against the Stripe API: PI showed `canceled` while the local row still said `AUTHORIZED`) — this was a data-integrity gap, not a financial-correctness one. Root cause, audited across all three: `cancelAuthorization()`, `capture()`, and `refund()` in `libs/services/payments/src/authorization-service.ts` each called the real Stripe operation and recorded success in `payments.operations` (the idempotency ledger), but none of them `UPDATE`d `payments.authorizations.status`. Fixed on `fix/qcf-011-012-stale-status-and-address-ui`: new migration `003_authorizations_status_widen.sql` widens the `status` CHECK constraint from `('SETUP_ONLY', 'AUTHORIZED')` to add `'CANCELLED'`, `'CAPTURED'`, `'REFUNDED'`; each of the three methods now `UPDATE`s `status` to the matching terminal value alongside its Stripe call. 3 new regression tests in `authorization-service.spec.ts` assert the status lands correctly for each method. |
| QCF-012 | M4-T01/T02, post-auth confirm screen | Should | On a customer's very first booking (empty address book), the guest-draft address is restored into component state but never surfaced in the UI — no pre-selected radio, no visible confirmation — so the screen shows an empty "Add a new address" form that reads as though re-entry is required | Fixed | Narrower than first suspected: once a saved address exists (e.g. after the first booking), it renders correctly as a pre-checked radio option on later visits — confirmed live. The gap was limited to the very first, no-saved-address-yet pass. Not a functional defect (CUS-001's literal requirement — slot/address not lost, no re-entry required — was technically satisfied, since `onConfirm()` already used the restored `choice` state and the booking submitted correctly without touching the empty form) but a real discoverability gap. Fixed on `fix/qcf-011-012-stale-status-and-address-ui`: `libs/ui/feature-booking/src/confirm.tsx` now shows a visible `"Using: <address>"` line when `choice.addressLine` is set but doesn't match any saved address (i.e. exactly the first-run case). New Playwright E2E `booking-smoke.spec.ts` ("the guest-draft address is visibly shown on return, not silently restored") drives the actual guest→register→confirm flow and asserts the address text is visible on return — confirmed it fails on the pre-fix code (`git stash` the fix, re-run, watch it fail on a 15s timeout) and passes on the fix. |
| QCF-013 | M4-T04, M4-T08, M4-T10 | Must | No UI exists anywhere for provider actions (accept/decline/complete/no-show/provider cancel) or for a customer to report a provider no-show — every M4 provider-action and no-show procedure this run had to be driven by direct API calls, never a click | Fixed | Confirmed by grepping `libs/ui/feature-provider/src`, `libs/ui/feature-booking/src`, `libs/ui/feature-account/src`, and `apps/web/app` for any of accept/decline/complete/no-show/cancel-booking affordances — zero matches on either side. Provider surface had only application/profile/services/availability/go-live/earnings; customer surface had only "leave a review"/"submit review". M4-P4's plan explicitly scoped this out, so it was a known, deliberate cut, not a regression — but it left QCF-010's routes unreachable by an actual user. Fixed on `fix/qcf-013-provider-booking-actions-ui`: new `GET /provider/me/bookings` (DTO-gated the same way `provider-view` already was — factored the gate into a shared `toProviderDTO` helper so it can't drift between the two routes), new `ProviderBookings` component (`libs/ui/feature-provider/src/bookings.tsx`) rendering the queue with accept/decline/complete/report-customer-no-show/cancel buttons per state, mounted on the provider page; `BookingHistory` (`libs/ui/feature-booking/src/history.tsx`) gained a "report provider no-show" button on `CONFIRMED` bookings past `slot_start` (gated by a new exported `canReportProviderNoShow` helper). en/he i18n for all new copy. Regression coverage: `provider-bookings-list.spec.ts` (API, DTO gate + cross-tenant isolation), `history.spec.ts` (unit test on the eligibility helper), `provider-booking-actions.spec.ts` (Playwright E2E — accept→complete, provider-cancel, and customer-reports-no-show, all driven by real clicks through the new UI, not API calls). Full suites green: API 65/65, `feature-booking` 8/8, web-e2e 16/16. |

---

## Triage

| Status | Meaning |
|---|---|
| **Open** | Confirmed real, not yet scheduled |
| **Fixed** | Shipped; branch/PR linked in Notes |
| **Backlog** | Converted into a tracked backlog item (link it in Notes); remove from Open review but keep the row for history |
| **Accepted** | Deliberately not fixing — named hole, documented where (plan doc, README, etc.) |
